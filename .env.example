# ===== MyWeb 环境变量示例 =====
# 复制为 .env 后根据个人服务器环境修改

# 开发端口（本地开发用）
FRONTEND_PORT=3000
BACKEND_PORT=3302
PORT=3302

# 生产容器的宿主机端口映射（docker-compose 使用）
# 将前端从 10010 改到 10011：只需改 FRONTEND_HOST_PORT=10011 并重建容器
FRONTEND_HOST_PORT=10010
BACKEND_HOST_PORT=3302

# 前端访问后端使用的 API 前缀（生产与开发统一）
VITE_API_BASE=/api

# CORS 允许来源（个人服务器建议填自己的对外访问地址与端口）
# 部署在 10010 时用：
CORS_ORIGIN=http://example:10010
# 若改到 10011，改为：
# CORS_ORIGIN=http://example:10011

# SQLite 数据文件（已由 compose 挂载）
DB_PATH=server/data/myweb.db

# 日志级别（info|debug|warn|error）
LOG_LEVEL=info