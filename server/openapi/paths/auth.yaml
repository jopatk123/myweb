verify:
  post:
    summary: Verify access password
    description: Validate the application access password and issue the app session cookie.
    tags:
      - Auth
    security: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              password:
                type: string
            required:
              - password
    responses:
      '200':
        description: Verified
      '401':
        description: Invalid password
      '503':
        description: Authentication is misconfigured for the current environment
      '429':
        description: Too many attempts

status:
  get:
    summary: Get access verification status
    description: Return whether access verification is required, whether the current request is authenticated, and whether server-side signing configuration is ready.
    tags:
      - Auth
    security: []
    responses:
      '200':
        description: Status returned

logout:
  post:
    summary: Clear access verification session
    description: Remove the current application access session cookie.
    tags:
      - Auth
    security: []
    responses:
      '200':
        description: Logged out
